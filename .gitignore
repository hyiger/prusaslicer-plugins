# Signing keys — the private key must never be committed. It lives outside this
# tree by default (~/.prusaslicer-plugin-keys/), but guard against a stray copy.
*private*.pem
*.key

# Signing artifacts, regenerated by `plugin sign` on every release.
*.zip
**/manifest.txt
**/manifest.sign
